setInterval(() => {    // It will continue executing the task after every 2 second 
    console.log('Hello World');
  },2000)
  console.log('I will run First');
  
  // Process stays alive unless
  // Kill Process Control + c
  
  
  // Every time when we have some Asyncrous action the funtion gets offLoaded and when the time comes we invoke callback
  